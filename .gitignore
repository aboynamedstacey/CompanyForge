__pycache__/
*.pyc

# Private — root-level generator, orchestration infrastructure, project config
/.claude/
/CLAUDE.md

# Private — study results (domain-specific research output)
domains/synthetic-audience/studies/

# Private — local Claude settings with machine-specific paths
**/.claude/settings.local.json

# Local working files
grok-summary.md
